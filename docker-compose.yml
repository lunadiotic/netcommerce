version: '3.4'

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: netcommerce-sqlserver
    environment:
      MSSQL_SA_PASSWORD: SuperSecretPass123!
      ACCEPT_EULA: 'Y'
    ports:
      - '1433:1433'
